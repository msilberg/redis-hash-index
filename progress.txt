# Ralph Progress Log
Started: (not yet run)
---

## Codebase Patterns

Seeded from the specification before the first iteration. Add to this section whenever you learn
something general and reusable — not story-specific details.

- Read `tasks/US-XXX.md` for the story you are on before touching code; `prd.json` holds only the
  acceptance criteria, the reasoning is in the task file.
- `docs/REDIS-SCHEMA.md` is the contract shared by all three services. Change it in one place or
  the demo silently stops proving anything.
- `SADD` creates a PERSISTENT key: `EXPIRE ... GT` alone will never set an expiry. Use `NX` then `GT`.
- Invalidation deletes values BEFORE removing index references, so a failed run is retry-safe.
- `SREM` the members you observed; never `DEL` the index set, or you discard concurrent writes.
- The v1 legacy pattern is `*::<userId>::*` — both delimiters, or it matches the index key too.
- Redis runs with `--maxmemory-policy noeviction`. If it evicts the fixture, the benchmark lies.
- Every Redis pipeline reply must be inspected. A silently dropped `SET` shrinks the keyspace and
  flatters the wrong side of the comparison.
- This dev machine has NO local `redis-cli`. Use `docker compose exec -T redis redis-cli ...`
  (or `docker run --rm --network host redis:7-alpine redis-cli -h localhost ...`) in scripts and
  self-checks instead of assuming a host binary.
- npm workspaces: root `package.json` has `workspaces: [packages/cache, services/*]`, each workspace
  has its own `tsconfig.json` extending `tsconfig.base.json` and a `typecheck` script. `make typecheck`
  / `make test` fan out with `npm run <x> --workspaces --if-present`.
- `make up` waits for health via `docker inspect --format '{{.State.Health.Status}}'` on
  `docker compose ps -q` — no `jq` dependency.
- Tests run on `node --import tsx --test "src/**/*.test.ts"` (Node 24, tsx devDep). Use `node:test` +
  `node:assert/strict`. `make test` now does `docker compose up -d redis` first — every service's
  tests need a real Redis, per the spec (no mocks).
- Import sibling modules WITHOUT a `.js` extension (`./index`), tsconfig uses `moduleResolution: node`.
- Consumers of `packages/cache` pass their ioredis client where `RedisClient` is expected and cast
  (`redis as unknown as RedisClient`) — the narrow interface is intentional, don't widen it.
- `EntityIndex.parse` returns `null` for a foreign/malformed key (never throws); `indexKeyFor` and
  `register` throw on bad segments / unknown category.
- `packages/cache` has a `prepare` script (`tsc`): a root `npm install` builds its `dist/`, so
  service workspaces typecheck/test against `dist/index.d.ts` with no manual build. Keep `main`/
  `types` at `dist/`; services `require()` the compiled JS.
- Service Dockerfiles: build context is the REPO ROOT (`context: .`), multi-stage — stage 1 copies
  root manifests + every workspace `package.json` + the service source, `npm ci` (runs cache's
  `prepare`), `npm run build --workspace <svc>`, `npm prune --omit=dev`; stage 2 copies
  `node_modules` + `packages/cache/dist` + `services/<svc>/dist`. Runtime = `node dist/index.js`,
  no tsx in prod. Compose healthcheck uses `node -e "fetch(...health...)"` — the image has no curl.
- Self-checks that `grep` the service source for `KEYS`/`scan(` will match your OWN comments too.
  Don't write those literal tokens in `services/*/src` — say "enumerate the keyspace" instead.
  EXCEPTION: `services/webhook` v1 path legitimately calls `redis.keys('*::<userId>::*')`.
- Services that accept a JSON body need `app.use(express.json())`. Long work: `void runJob(...)`
  then `res.status(202)` immediately; the worker owns all its own error handling and sets
  `job.state`. Job store is an in-memory `Map` created INSIDE `createApp` so tests are isolated.
- The v1 KEYS pattern keeps both `::` so it never matches the index key (name ends at the id).
  v1 therefore leaves index sets behind (they TTL out); only v2 empties them via `SREM`.
- Service tests: live HTTP via `createApp(redis)` on `app.listen(0)` + global `fetch`; modules are
  `commonjs` so use `__dirname`, never `import.meta.url`.
- `node:test` has NO default per-test timeout: a promise that never resolves hangs `make test`
  forever (looks like a slow suite). Two ways this bites: (a) `server.close()` waits on undici
  keep-alive sockets from `fetch` — call `server.closeAllConnections()` first; (b) awaiting
  `once(emitter, "done")` after triggering fast async work that already emitted — poll state instead.
- benchmark service = Express app + `http.Server` + `ws` at `/ws`. `createApp({seeder})` returns the
  app; `attachWebSocket(server, seeder)` returns a hub with `broadcast` (US-006/007 reuse it).
- The seeder is deterministic from `SEED_VALUE` (mulberry32 + an order-independent `hashInt`).
  `services/benchmark/src/fixture.ts` is the single generator — changing it diverges every fixture.
- Seeder writes with `flushdb()` first, then asserts `DBSIZE === cacheKeys + indexKeys` BEFORE
  writing `seed::marker`. So live DBSIZE after a completed seed is that total **+ 1** (the marker).
- `SEED_KEYS` counts cache *records*; users ≈ SEED_KEYS/2 (`averageVariants` = 2). Actual cacheKeys
  is the sum of `1+(hash(i)%3)` and lands a hair under SEED_KEYS — status/marker report the real count.
- Compose forwards `SEED_KEYS`/`SEED_VALUE` from the shell env, so `SEED_KEYS=50000 make up` works.

## 2026-09-09 - US-001
- Scaffolded the workspace: root `package.json` (npm workspaces, private), `tsconfig.base.json`
  (strict, noUncheckedIndexedAccess, exactOptionalPropertyTypes, noImplicitOverride, ES2022,
  commonjs, declaration), `docker-compose.yml` with the `redis:7-alpine` service on 6379
  (`--maxmemory-policy noeviction --save '' --appendonly no`) + healthcheck, and a `Makefile`
  with up/down/logs/typecheck/test/seed/reset/verify.
- Created placeholder workspaces `packages/cache`, `services/test-api`, `services/webhook`,
  `services/benchmark`, each with `package.json`, `tsconfig.json`, and a stub `src/index.ts`.
- `make verify` is a deliberate stub that exits 1 until US-008 implements it.
- Files changed: package.json, package-lock.json, tsconfig.base.json, docker-compose.yml, Makefile,
  packages/cache/*, services/*/{package.json,tsconfig.json,src/index.ts}.
- Self-check executed: `make up` (redis reports healthy), `redis-cli ping` -> PONG (via docker),
  CONFIG GET confirms noeviction / save '' / appendonly no, `make typecheck` exit 0,
  `make test` exit 0, `make down` exit 0.
- **Learnings for future iterations:**
  - No host `redis-cli` — always go through the container (see Codebase Patterns).
  - `tsc -p tsconfig.json` needs at least one input file, hence the `export {};` stubs; delete them
    when the real modules land.
  - Compose has no top-level `version:` key (obsolete in Compose v2).
  - Keep the three Node service Dockerfiles OUT until their stories (US-003/004/005).
---

## 2026-09-09 - US-002
- Implemented `packages/cache` — the shared `EntityIndex` (class) with `indexKeyFor`, `parse`,
  `register`, `invalidateEntities`, `prune`, plus a narrow `RedisClient` / `RedisMulti` /
  `RedisPipeline` interface listing only the commands used.
  - `register`: one MULTI — `SADD`, `EXPIRE ttl NX`, `EXPIRE ttl GT` — every reply tuple inspected;
    returns whether the member was newly added. TTL validated as whole seconds 1..2592000 BEFORE
    any write (rejected call writes nothing).
  - `invalidateEntities`: per entity `SMEMBERS` -> `UNLINK` values (batched, default 500) ->
    `SREM` observed members (batched). Never `DEL`s the set. Deletes every member unconditionally,
    no timestamp filter. Entities processed with bounded concurrency (default 4).
  - `prune`: pipelined `EXISTS` per member, `SREM` the missing ones. Maintenance only, not the
    delete path.
  - `parse` is structural + segment regex `[A-Za-z0-9_.-]+` + category check; returns null, never
    throws. `indexKeyFor` throws on a bad segment or unknown category.
- Tests (`src/index.test.ts`, `node:test` + real Redis on DB 15): key building / parse-null cases;
  NX+GT sets then extends but never shortens; rejected TTL writes nothing; full invalidate removes
  values + refs; injected `UNLINK` failure leaves the index intact and a retry finishes; a member
  added between SMEMBERS and SREM survives; 200,000 members invalidate without a RangeError
  (proves batching, not spreading); prune drops dead refs only. 12/12 pass.
- Files changed: packages/cache/{src/index.ts,src/index.test.ts,package.json,CLAUDE.md},
  Makefile (test target now brings up redis), package-lock.json.
- Self-check executed: `make typecheck` exit 0; `make test` -> 12 pass / 0 fail.
- **Learnings for future iterations:**
  - `EXPIRE key ttl 'NX'` / `'GT'` works through ioredis `multi()` untyped-passthrough; the narrow
    `RedisMulti.expire(key, seconds, 'NX'|'GT')` signature is enough and ioredis is cast in tests.
  - `redis.multi().exec()` resolves to `[err, reply][] | null`; `null` means EXECABORT — treat as
    an error. A queued command that fails at runtime still shows up as `[err, ...]` in the array,
    Redis does NOT roll the rest back.
  - The 200k-member test takes ~2.3s against local compose Redis; keep an eye on total test time
    as more services add real-Redis tests.
---

## 2026-09-09 - US-003
- Implemented `services/test-api` — the innocent-bystander reader.
  - `src/app.ts` `createApp(redis)` returns an Express app: `GET /health` -> `{ok:true}`;
    `GET /entitlement/:userId` validates `^u_\d{7}$` (400 otherwise), builds the index key via
    `EntityIndex.indexKeyFor("demo","activeSubscription",userId)`, then `SMEMBERS` -> `MGET`,
    timing only the Redis round trip with `process.hrtime.bigint()`. Responds
    `{userId, hit: values.some(Boolean), variants: values.filter(Boolean).length, latencyMs}`.
    `hit:false`/`variants:0` for an unseeded/invalidated user is the correct answer, no fallback.
  - `src/index.ts` opens this service's OWN ioredis connection and wires the app on `PORT` (3001),
    with SIGTERM/SIGINT graceful shutdown.
  - `src/config.ts` — `TENANT`/`CATEGORY` constants + `loadConfig` (PORT, REDIS_URL).
  - Narrow `RedisReader` interface (smembers/mget only); ioredis cast at the composition root.
- `packages/cache/package.json` gained a `prepare` script so consumers get `dist/` on `npm install`.
- Dockerfile: multi-stage `node:22-alpine`, build context repo root, `npm ci` + build + `npm prune
  --omit=dev`, runtime copies node_modules + both dists, `USER node`, runs compiled JS.
- `docker-compose.yml`: `test-api` service on 3001, `depends_on: redis: service_healthy`, node-fetch
  healthcheck. Added `.dockerignore`.
- Files changed: services/test-api/{package.json,Dockerfile,src/{app,index,config,app.test}.ts},
  packages/cache/{package.json,CLAUDE.md}, services/CLAUDE.md (new), docker-compose.yml,
  .dockerignore, package.json/package-lock.json.
- Self-check executed: `make typecheck` exit 0; `make test` -> cache 12/12, test-api 4/4;
  `make up` -> test-api container reports healthy; `curl /health` -> `{"ok":true}`;
  `curl /entitlement/u_0000001` -> 200 with `latencyMs` (and `hit:true`/`variants:1` after a
  manual SET+SADD); `curl /entitlement/bogus` -> 400;
  `grep -rn "KEYS\|scan(" services/test-api/src` -> no matches.
- **Learnings for future iterations:**
  - See the new Codebase Patterns entries: the cache `prepare` build, the multi-stage service
    Dockerfile recipe, the grep-your-own-comments trap, and the live-HTTP test style.
  - `req.params.userId` is `string | undefined` under `noUncheckedIndexedAccess` — guard it.
  - Express 4 does not catch async handler rejections; `readEntitlement(...).then(...).catch(next-ish)`
    or a try/catch is required or an error becomes an unhandled rejection.
  - The runtime image has global `fetch` (node 22) but NO `curl` — compose healthchecks must use
    `node -e`.
---

## 2026-09-09 - US-004
- Implemented `services/webhook` — both invalidation paths behind two endpoints.
  - `src/app.ts` `createApp(redis)`: `GET /health`; `POST /v1/invalidate` and `POST /v2/invalidate`
    both take `{userIds:[...]}` (validated `^u_\d{7}$`, 400 otherwise), create a `Job` in an
    in-memory `Map`, fire `void runJob(...)` and return `202 {jobId,mode,total}` at once.
  - `runJob` walks users one at a time, checking `job.state === "stopped"` BETWEEN users. v1:
    `redis.keys('*::<userId>::*')` then `UNLINK` the matches in batches of 500. v2: delegates each
    user to `packages/cache` `EntityIndex.invalidateEntities(TENANT,CATEGORY,[userId])`. `removed`
    counter = UNLINK replies (v1) / `valuesUnlinked` (v2).
  - `GET /jobs/:id` returns the whole Job (`{jobId,mode,state,total,processed,removed,startedAt,
    finishedAt,error?}`), 404 if unknown. `POST /jobs/:id/stop` flips a running job to `stopped`
    (no-op otherwise) and returns `{jobId,state}`.
  - `src/index.ts` opens webhook's OWN ioredis connection on PORT 3002 with SIGTERM/SIGINT drain.
  - `src/config.ts` — TENANT/CATEGORY/BATCH_SIZE consts + `loadConfig` (PORT default 3002).
- `Dockerfile` mirrors test-api's multi-stage recipe (repo-root context). `docker-compose.yml`
  gains `webhook` on 3002, `depends_on redis: service_healthy`, node-fetch healthcheck.
- Integration test (`src/app.test.ts`, real Redis DB 15): health; ~80 users / ~200 keys split into
  disjoint v1 and v2 groups + one bystander — both jobs poll to `done`, every cache key for both
  groups gone, every v2 index key gone, bystander (id + index) untouched; stop over 4000 unseeded
  ids lands `stopped` with `processed < total`; 400s for bad bodies; 404s for unknown jobs.
- Files changed: services/webhook/{package.json,Dockerfile,src/{app,index,config,app.test}.ts},
  services/CLAUDE.md, docker-compose.yml, package-lock.json, prd.json, progress.txt.
- Self-check executed: `make typecheck` exit 0; `make test` -> cache 12/12, test-api 4/4,
  webhook 5/5; `make up` -> webhook container healthy; `curl :3002/health` -> `{"ok":true}`;
  `curl -XPOST :3002/v1/invalidate -d '{"userIds":["u_0000001"]}'` -> instant 202 with jobId,
  `GET /jobs/<id>` -> `state:"done" processed:1 removed:1`, cache key gone, index key still present;
  v2 path -> index key emptied; big v1 job -> stop -> `state:"stopped" processed:74/4000`;
  bad body -> 400. `make down` clean.
- **Learnings for future iterations:**
  - See new Codebase Patterns entries (express.json, void runJob + 202, in-createApp job Map,
    v1 leaves index sets behind).
  - `npm install` reported "up to date" after adding webhook deps — express/ioredis/tsx were
    already deduped from test-api — but package-lock.json still gained the webhook workspace refs.
  - `redis.unlink()` with zero args throws; the batch loop skips empty match lists naturally.
---

## 2026-09-09 - US-005
- Implemented `services/benchmark` — the deterministic seeder + its API and WebSocket.
  - `src/prng.ts` — `mulberry32` (float stream) + `hashInt` (order-independent integer avalanche,
    salted by `SEED_VALUE`). No dependency.
  - `src/fixture.ts` — the single fixture generator: `userCount` (≈ SEED_KEYS/2), `userIdFor`
    (`u_` + 7 digits), `variantsFor` (`1 + hash%3`), `expectedTotals`, and `generateUsers` yielding
    `{userId, indexKey, records:[{cacheKey,value}]}`. Cache key = `test-api::demo::activeSubscription::
    <userId>::{"v":N}`; value = `{userId,planId,status,renewsAt,seats}` (planId cycled over the four
    plans, seats/renewsAt from the hash). Index key comes from `EntityIndex.indexKeyFor`.
  - `src/seeder.ts` — `Seeder` (EventEmitter) state machine `idle|seeding|ready|failed`.
    `start()` runs in the background: `flushdb`, then pipelines of ~20k commands
    (`SET ... EX 3600`, `SADD` all members, `EXPIRE NX`, `EXPIRE GT`), inspecting **every** reply
    tuple (error ⇒ abort ⇒ `failed`). On completion asserts `DBSIZE === cacheKeys + indexKeys`
    BEFORE writing `seed::marker` (JSON of counts + completedAt). `init()` loads the marker so a
    restart reports `ready` without reseeding. `reset()` = `flushdb` + back to `idle`. Emits
    `{t:'seed-progress',done,total,percent}` on a 1s `setInterval` plus at start/finish.
  - `src/app.ts` — `GET /health`, `GET /`, `GET /api/seed/status`, `POST /api/seed` (202, or 409
    while seeding), `POST /api/seed/reset`.
  - `src/ws.ts` — `attachWebSocket(server, seeder)` → hub with `broadcast`; forwards `progress`
    events to `/ws` clients and sends a snapshot frame on connect.
  - `src/index.ts` — own ioredis connection, `http.Server` + ws, SIGTERM/SIGINT drain.
  - `Dockerfile` mirrors the other services; `docker-compose.yml` gains `benchmark` on 3000 with a
    node-fetch healthcheck and `SEED_KEYS`/`SEED_VALUE` env passthrough.
- Tests (`src/fixture.test.ts` pure, `src/seeder.test.ts` real Redis DB 15): PRNG determinism,
  fixture byte-identity (same seed) / divergence (different seed), key/value shape, full seed +
  DBSIZE + marker, armed TTL on both string and set (NX/GT), reseed byte-identity, `init()` from a
  marker, `reset`, concurrent-`start` rejection, a pipeline error ⇒ `failed`, the HTTP 202/409 flow,
  and WS `seed-progress` frames ending at percent 1. 16/16.
- Files changed: services/benchmark/{package.json,Dockerfile,tsconfig.json,src/{config,prng,fixture,
  seeder,app,ws,index}.ts,src/{fixture,seeder}.test.ts}, docker-compose.yml, services/CLAUDE.md,
  package.json/package-lock.json (added `ws` + `@types/ws`), prd.json, progress.txt.
- Self-check executed: `make typecheck` exit 0; `make test` exit 0 (cache 12, test-api 4, webhook 5,
  benchmark 16); `SEED_KEYS=50000 make up` → benchmark healthy; `POST /api/seed` → `ready` in ~1.3s
  (< 30s), status `progress:1 users:25000 cacheKeys:49961 indexKeys:25000`; DBSIZE 74962 =
  49961+25000+1 (marker); reseed → byte-identical value + identical DBSIZE; `seed::marker` present;
  `POST /api/seed/reset` → DBSIZE 0, status `idle`; second `POST /api/seed` mid-seed → 409;
  `docker compose restart benchmark` → status `ready` (no reseed); a live `/ws` client received
  `{t:'seed-progress'}` frames.
- **Learnings for future iterations:**
  - See the new Codebase Patterns entries (node:test has no timeout; closeAllConnections;
    poll-don't-race; deterministic seeder; DBSIZE is total + 1 for the marker).
  - A 50k seed finishes in ~1s, faster than the 1s progress ticker — you only see start/finish
    frames at that size. The default 2M seed is where the per-second frames matter.
  - `EXPIRE key ttl "NX"|"GT"` works through an ioredis *pipeline* (not just multi), replies inspected.
  - US-006 run driver can `import { attachWebSocket }` / reuse `hub.broadcast` for `sample` frames,
    and draw eviction user ids from `userIdFor(0..userCount-1)`.
---
